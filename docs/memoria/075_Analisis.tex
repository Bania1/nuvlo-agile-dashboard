\chapter{Análisis del sistema}

Este capítulo recoge los modelos de análisis utilizados para representar la estructura conceptual de \textit{Nuvlo} y la información que debe persistirse. A diferencia del capítulo de requisitos, que describe qué debe ofrecer el sistema desde el punto de vista funcional, estos diagramas ayudan a razonar sobre el dominio, sus entidades principales y las relaciones entre ellas.

\section{Diagrama de clases}

El diagrama de clases muestra la estructura estática principal del sistema y las relaciones entre los conceptos del dominio. En este caso se representa una vista de análisis previa a la implementación, por lo que las clases describen entidades y responsabilidades del sistema sin depender de una tecnología concreta.

\begin{landscape}
\begin{figure}[p]
    \centering
    \makebox[\linewidth][c]{%
        \includegraphics[width=1.12\linewidth,height=0.86\textheight,keepaspectratio]{diagrams/clases-dominio-sistema.png}%
    }
    \caption{Diagrama de clases del sistema}
    \label{fig:clases-dominio-sistema}
\end{figure}
\end{landscape}

La Figura~\ref{fig:clases-dominio-sistema} se interpreta a partir de los verbos situados sobre cada relación. El triángulo del verbo indica la dirección de lectura y las cardinalidades de los extremos concretan cuántas instancias pueden participar en cada asociación. En cuanto a la notación \textit{UML}, siglas de \textit{Unified Modeling Language}, según la especificación oficial de \textit{OMG}, siglas de \textit{Object Management Group}~\cite{OMGUML251}, el rombo blanco representa una agregación, es decir, una relación \textit{todo-parte} débil en la que la \textit{parte} puede existir de forma independiente. El rombo negro representa una composición, una relación \textit{todo-parte} fuerte en la que la \textit{parte} depende del ciclo de vida del \textit{todo}.

Además de identificar las clases, es necesario indicar qué papel desempeña cada una dentro del dominio. La Tabla~\ref{tab:responsabilidades-clases} resume las responsabilidades principales y los atributos o conceptos más relevantes asociados a cada clase. Estos atributos no pretenden sustituir al diseño físico de la base de datos, sino aclarar qué información mínima necesita manejar el sistema para cumplir los casos de uso definidos.

\begin{landscape}
{\scriptsize
\renewcommand{\arraystretch}{1.08}
\begin{longtable}{>{\raggedright\arraybackslash}p{4.6cm}>{\raggedright\arraybackslash}p{7.6cm}>{\raggedright\arraybackslash}p{9.8cm}}
\caption{Responsabilidades y atributos principales de las clases del dominio}
\label{tab:responsabilidades-clases}\\
\toprule
\textbf{Clase} & \textbf{Responsabilidad principal} & \textbf{Atributos o información principal} \\
\midrule
\endfirsthead
\toprule
\textbf{Clase} & \textbf{Responsabilidad principal} & \textbf{Atributos o información principal} \\
\midrule
\endhead
\bottomrule
\endfoot
\texttt{UsuarioAutenticado} & Representar al usuario que accede a \textit{Nuvlo} y delimitar la propiedad de sesiones, ámbitos de análisis y configuraciones. & Identificador interno, identificador de cuenta \textit{Atlassian}, nombre visible, correo electrónico, fecha de alta, fecha de último acceso y estado de la cuenta. \\
\texttt{SesionAtlassian} & Mantener la información necesaria para operar en nombre del usuario autenticado sin pedir credenciales locales. & Identificador de sesión, usuario asociado, \textit{cloudId}, \textit{scopes} concedidos, fecha de creación, fecha de expiración y referencia segura a los \textit{tokens}. \\
\texttt{AmbitoAnalisis} & Definir el subconjunto de datos sobre el que se calculan métricas y se muestran resultados. & Nombre del ámbito, usuario propietario, proyectos incluidos, tablero o \textit{Sprint} seleccionado, rango temporal, filtros activos y fecha de última sincronización. \\
\texttt{ConfiguracionAnalisis} & Recoger los criterios que modifican la interpretación de los datos y el cálculo de métricas. & Estados de inicio y fin, estados considerados trabajo en curso, campo de esfuerzo, percentiles visibles, agrupación temporal y criterios de exclusión. \\
\texttt{ProyectoJira} & Representar un proyecto importado desde \textit{Jira} y servir como contenedor lógico de tableros e incidencias. & Clave del proyecto, nombre, identificador \textit{Jira}, URL de origen, tipo de proyecto y fecha de última actualización local. \\
\texttt{TableroJira} & Modelar un tablero de trabajo de \textit{Jira} asociado a un proyecto y usado para organizar la consulta de \textit{Sprints}. & Identificador \textit{Jira} del tablero, nombre, tipo de tablero, proyecto asociado y fecha de sincronización. \\
\texttt{Sprint} & Representar una iteración temporal utilizada para calcular \textit{Velocity} y analizar evolución por periodos. & Identificador \textit{Jira} del \textit{Sprint}, nombre, estado, fecha de inicio, fecha de fin, fecha de cierre y tablero asociado. \\
\texttt{IncidenciaJira} & Representar cada unidad de trabajo sincronizada desde \textit{Jira} y aportar los datos base para métricas de flujo. & Clave, resumen, tipo, estado actual, prioridad, estimación, fecha de creación, fecha de resolución, proyecto asociado y responsable si está disponible. \\
\texttt{TransicionEstado} & Registrar los cambios de estado de una incidencia para reconstruir su recorrido y calcular tiempos. & Incidencia asociada, estado origen, estado destino, fecha de transición y origen de la información sincronizada. \\
\texttt{SincronizacionDatos} & Controlar cada proceso de actualización desde \textit{Jira} o desde el conjunto de datos de demostración. & Ámbito afectado, instante de inicio y fin, estado de ejecución, número de elementos procesados, errores detectados y resumen del resultado. \\
\texttt{MetricaCalculada} & Almacenar el resultado de un cálculo de métrica para un ámbito, periodo y configuración concreta. & Tipo de métrica, valor agregado, periodo, percentiles, número de incidencias consideradas, fecha de cálculo y configuración utilizada. \\
\texttt{ReglaAlerta} & Definir condiciones configurables que permiten avisar al usuario ante valores relevantes de una métrica. & Métrica objetivo, operador, umbral, severidad, estado activo/inactivo, ámbito asociado y fecha de creación. \\
\texttt{AlertaActivada} & Representar una ocurrencia concreta generada cuando una regla cumple su condición. & Regla origen, métrica evaluada, valor observado, severidad, mensaje, fecha de activación y estado de revisión. \\
\texttt{LogSistema} & Conservar eventos relevantes para trazabilidad, depuración y explicación de operaciones internas. & Tipo de evento, severidad, mensaje, usuario o ámbito asociado, fecha de registro, origen del evento y fecha prevista de expiración. \\
\end{longtable}
}
\end{landscape}

A partir de estas responsabilidades se observa una separación entre clases de identidad y configuración, clases procedentes del dominio \textit{Jira}, clases de cálculo y clases de trazabilidad. Esta división facilita que el sistema pueda sincronizar información externa, conservar una representación local estable y calcular métricas sin depender de una llamada directa a \textit{Jira} en cada consulta del usuario.

\FloatBarrier
\section{Diagrama entidad-relación de la base de datos}

El diagrama entidad-relación describe la estructura lógica prevista para la persistencia del sistema. A diferencia del diagrama de clases, que representa conceptos del dominio y responsabilidades de análisis, este modelo se centra en tablas, claves primarias, claves foráneas y cardinalidades. Para facilitar su lectura se emplean entidades rectangulares con sus atributos internos y relaciones representadas mediante rombos, siguiendo la forma habitual de los diagramas entidad-relación y las formas disponibles en draw.io para modelos de bases de datos~\cite{DrawioERTables}. Cada rombo contiene un verbo que describe la relación y las cardinalidades se sitúan junto a los extremos conectados. Los nombres de tablas y columnas se han planteado en minúsculas y con \texttt{snake\_case}, siguiendo criterios habituales de estilo SQL~\cite{HolywellSQLStyle}; además, se evitan estructuras innecesariamente complejas para mantener un diseño comprensible y normalizado, de acuerdo con las recomendaciones generales de diseño relacional recogidas por Karwin~\cite{Karwin2010SQLAntipatterns}.

\begin{figure}[htbp]
    \centering
    \makebox[\textwidth][c]{\includegraphics[width=1.25\textwidth]{diagrams/entidad-relacion-base-datos.png}}
    \caption{Diagrama entidad-relación de la base de datos}
    \label{fig:entidad-relacion-base-datos}
\end{figure}

La Figura~\ref{fig:entidad-relacion-base-datos} toma como referencia las entidades principales del diagrama de clases, pero las adapta a un modelo persistente. Las tablas puente \texttt{ambito\_proyecto} e \texttt{incidencia\_sprint} permiten resolver relaciones de muchos a muchos sin duplicar información ni forzar dependencias artificiales.

\begin{itemize}
    \item \texttt{usuario\_autenticado}, \texttt{sesion\_atlassian}, \texttt{ambito\_analisis} y \texttt{configuracion\_analisis}: almacenan la información del usuario, sus sesiones de \textit{Atlassian} y los criterios de análisis definidos para cada ámbito. Un usuario puede iniciar varias sesiones y definir varios ámbitos; cada ámbito mantiene una configuración de análisis.
    \item \texttt{proyecto\_jira}, \texttt{tablero\_jira}, \texttt{sprint}, \texttt{incidencia\_jira} y \texttt{transicion\_estado}: representan los datos sincronizados desde \textit{Jira}. Un proyecto puede contener varios tableros e incidencias, un tablero organiza \textit{Sprints} y una incidencia registra sus transiciones de estado para calcular métricas de flujo.
    \item \texttt{ambito\_proyecto} e \texttt{incidencia\_sprint}: actúan como tablas intermedias. La primera asocia ámbitos de análisis con proyectos \textit{Jira}; la segunda relaciona incidencias con \textit{Sprints}, evitando una relación directa rígida cuando una incidencia pueda aparecer en más de una iteración.
    \item \texttt{sincronizacion\_datos}, \texttt{metrica\_calculada}, \texttt{regla\_alerta}, \texttt{alerta\_activada} y \texttt{log\_sistema}: guardan la actividad interna del sistema. Las sincronizaciones actualizan un ámbito, las métricas se calculan a partir de la configuración vigente, las reglas evalúan dichas métricas y las alertas y \textit{logs} conservan la trazabilidad del funcionamiento.
\end{itemize}

Para completar la lectura del modelo, la Tabla~\ref{tab:campos-er} recoge los campos principales asociados a cada entidad. Se muestran los atributos necesarios para comprender la persistencia lógica del sistema, no todos los detalles internos de implementación. La clave primaria de cada tabla se representa mediante \texttt{id}; las referencias a otras entidades se indican mediante campos terminados en \texttt{\_id} o mediante identificadores externos de \textit{Jira} cuando proceda.

\begin{landscape}
{\footnotesize
\begin{longtable}{>{\raggedright\arraybackslash}p{5.2cm}>{\raggedright\arraybackslash}p{12.0cm}>{\raggedright\arraybackslash}p{6.0cm}}
\caption{Campos principales del modelo entidad-relación}
\label{tab:campos-er}\\
\toprule
\textbf{Tabla} & \textbf{Campos principales} & \textbf{Observaciones} \\
\midrule
\endfirsthead
\toprule
\textbf{Tabla} & \textbf{Campos principales} & \textbf{Observaciones} \\
\midrule
\endhead
\bottomrule
\endfoot
\texttt{usuario\_autenticado} & \texttt{id}, \texttt{atlassian\_account\_id}, \texttt{email}, \texttt{display\_name}, \texttt{created\_at}, \texttt{updated\_at}. & Identifica al usuario mediante \textit{Atlassian}. No almacena contraseña local, ya que la autenticación se delega en el proveedor externo. \\
\texttt{sesion\_atlassian} & \texttt{id}, \texttt{usuario\_id}, \texttt{cloud\_id}, \texttt{site\_name}, \texttt{site\_url}, \texttt{encrypted\_access\_token}, \texttt{encrypted\_refresh\_token}, \texttt{scopes}, \texttt{expires\_at}, \texttt{created\_at}, \texttt{updated\_at}. & Guarda la autorización concedida por \textit{Atlassian}. Los \textit{tokens} se almacenan cifrados y se asocian al sitio \textit{Jira} autorizado. \\
\texttt{ambito\_analisis} & \texttt{id}, \texttt{usuario\_id}, \texttt{proyecto\_id}, \texttt{name}, \texttt{board\_jira\_id}, \texttt{sprint\_jira\_id}, \texttt{labels}, \texttt{issue\_types}, \texttt{date\_from}, \texttt{date\_to}, \texttt{start\_statuses}, \texttt{done\_statuses}, \texttt{effort\_field}, \texttt{percentile\_marks}, \texttt{last\_sync\_at}. & Define el contexto sobre el que se consultan métricas y aplica filtros de proyecto, tablero, \textit{Sprint}, etiquetas, tipos y fechas. \\
\texttt{configuracion\_analisis} & \texttt{id}, \texttt{ambito\_id}, \texttt{start\_statuses}, \texttt{done\_statuses}, \texttt{wip\_statuses}, \texttt{effort\_field}, \texttt{percentile\_marks}, \texttt{grouping}. & En la implementación actual estos campos se integran dentro del ámbito de análisis para simplificar el modelo físico, aunque conceptualmente representan la configuración del cálculo. \\
\texttt{proyecto\_jira} & \texttt{id}, \texttt{cloud\_id}, \texttt{jira\_id}, \texttt{key}, \texttt{name}, \texttt{avatar\_url}. & Conserva la información mínima del proyecto sincronizado desde \textit{Jira Cloud}. La combinación \texttt{cloud\_id} y \texttt{jira\_id} evita duplicados entre sitios. \\
\texttt{tablero\_jira} & \texttt{id}, \texttt{jira\_id}, \texttt{name}, \texttt{type}, \texttt{project\_id}. & Representa tableros \textit{Scrum} o \textit{Kanban} asociados a un proyecto importado. \\
\texttt{sprint} & \texttt{id}, \texttt{jira\_id}, \texttt{name}, \texttt{state}, \texttt{start\_date}, \texttt{end\_date}, \texttt{complete\_date}, \texttt{board\_id}. & Permite agrupar incidencias por iteración y calcular métricas por periodo cuando \textit{Jira} proporciona esa información. \\
\texttt{incidencia\_jira} & \texttt{id}, \texttt{jira\_id}, \texttt{key}, \texttt{summary}, \texttt{issue\_type}, \texttt{status}, \texttt{status\_category}, \texttt{priority}, \texttt{assignee}, \texttt{labels}, \texttt{story\_points}, \texttt{jira\_created\_at}, \texttt{jira\_updated\_at}, \texttt{project\_id}, \texttt{sprint\_id}. & Es la entidad central del análisis. Aporta estado, tipo, esfuerzo, fechas y vínculos con proyecto y \textit{Sprint}. \\
\texttt{transicion\_estado} & \texttt{id}, \texttt{issue\_id}, \texttt{from\_status}, \texttt{to\_status}, \texttt{happened\_at}. & Registra el historial de cambios de estado necesario para calcular \textit{Lead Time}, \textit{Cycle Time} y evolución del flujo. \\
\texttt{metrica\_calculada} & \texttt{id}, \texttt{scope\_id}, \texttt{type}, \texttt{value}, \texttt{unit}, \texttt{period\_key}, \texttt{metadata}, \texttt{created\_at}. & Guarda resultados calculados para un ámbito concreto. El campo \texttt{metadata} permite conservar detalles auxiliares como percentiles, conteos o valores por estado. \\
\texttt{regla\_alerta} & \texttt{id}, \texttt{scope\_id}, \texttt{metric\_type}, \texttt{operator}, \texttt{threshold}, \texttt{enabled}, \texttt{created\_at}. & Define condiciones evaluables sobre métricas. Permite activar o desactivar reglas sin eliminar su definición. \\
\texttt{alerta\_activada} & \texttt{id}, \texttt{rule\_id}, \texttt{status}, \texttt{value}, \texttt{triggered\_at}, \texttt{resolved\_at}. & Registra cada activación de una regla, el valor que la produjo y su estado de resolución. \\
\texttt{sincronizacion\_datos} & \texttt{id}, \texttt{scope\_id}, \texttt{status}, \texttt{started\_at}, \texttt{finished\_at}, \texttt{imported}, \texttt{error\_message}. & Permite auditar cada sincronización real o simulada, indicando resultado, volumen importado y errores si existen. \\
\texttt{log\_sistema} & \texttt{id}, \texttt{user\_id}, \texttt{event\_type}, \texttt{message}, \texttt{metadata}, \texttt{created\_at}. & Recoge eventos de actividad, autenticación, sincronización, cálculo, alertas y errores. Se considera información retenible por política de conservación. \\
\texttt{ambito\_proyecto} & \texttt{ambito\_id}, \texttt{proyecto\_id}. & Tabla puente conceptual para asociar un ámbito con varios proyectos. En la implementación inicial se admite un proyecto principal por ámbito. \\
\texttt{incidencia\_sprint} & \texttt{incidencia\_id}, \texttt{sprint\_id}. & Tabla puente conceptual para reflejar que una incidencia puede analizarse en más de una iteración. En la implementación inicial se conserva el \textit{Sprint} principal sincronizado. \\
\end{longtable}
}
\end{landscape}

Esta descripción de campos permite relacionar el modelo lógico con la implementación posterior. Algunas entidades conceptuales se han simplificado en el esquema físico inicial para reducir complejidad, pero se mantienen en el análisis porque ayudan a explicar la evolución natural del sistema si se amplía el soporte de múltiples proyectos por ámbito o de incidencias presentes en varios \textit{Sprints}.

\FloatBarrier
